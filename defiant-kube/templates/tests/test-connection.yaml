apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "defiant-kube.fullname" . }}-test-connection"
  labels:
    {{- include "defiant-kube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "defiant-kube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
